# alvenqis-devnet

Status: Draft / Private Devnet / Prototype

This folder contains local-only devnet assets for Alvenqis Network.

Current contents:
- `config/devnet.toml` for private devnet parameters;
- `data/` for local JSONL block storage produced by `alvenqis-node`.

Rules:
- local developer use only;
- no public testnet data;
- no mainnet data;
- no secrets, keys, seeds or `.env` files.

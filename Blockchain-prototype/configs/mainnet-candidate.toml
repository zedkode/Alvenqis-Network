network = "mainnet-candidate"
network_name = "alvenqis-mainnet"
network_id = "alvenqis-mainnet-candidate"
human_name = "Alvenqis Mainnet Candidate"
status_label = "Planned / Mainnet Candidate"
block_time_seconds = 60
difficulty_leading_zero_bits = 16
ticker = "ALVE"
address_prefix = "alve"
max_supply = "60000000"
halving_interval = 1576800
initial_block_reward = "19.02587519"
default_rpc_port = 10787
default_p2p_port = 20787
p2p_bind_host = "0.0.0.0"
p2p_listen_port = 20787
max_peers = 128
max_mempool_transactions = 2048
genesis_config_path = "configs/genesis.mainnet-candidate.toml"
# Frozen review metadata: changing this value invalidates the published review hash.
genesis_review_human_name = "Alvenqis Mainnet Candidate"
genesis_approval_path = "docs/release/GENESIS_APPROVAL.mainnet-candidate.json"
chain_magic_hex = "414c4d43"
allow_mainnet_candidate = true
data_dir = ".alvenqis-mainnet/chain"
mempool_dir = ".alvenqis-mainnet/mempool"
indexer_dir = ".alvenqis-mainnet/indexer"
# Public bootstrap for external full nodes / additional VPS peers.
# dimitrius.ombra-net.com is currently protocol-incompatible and must not be
# used as a seed until it runs the same P2P protocol_version and network_id.
seed_nodes = ["node.dohotstudio.com:20787"]

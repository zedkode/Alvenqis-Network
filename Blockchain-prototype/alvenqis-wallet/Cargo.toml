[package]
name = "alvenqis-wallet"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"
description = "Draft local devnet wallet CLI for Alvenqis Network"

[dependencies]
aes-gcm = "0.10"
argon2 = "0.5"
base64 = "0.22"
clap = { version = "4.5", features = ["derive"] }
dirs = "5"
rand = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
alvenqis-core = { path = "../alvenqis-core" }
alvenqis-node = { path = "../alvenqis-node" }
alvenqis-sdk-rust = { path = "../alvenqis-sdk-rust", features = ["native", "blocking"] }
zeroize = "1"

[dev-dependencies]
tempfile = "3"

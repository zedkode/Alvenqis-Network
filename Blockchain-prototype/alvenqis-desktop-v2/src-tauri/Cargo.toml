[package]
name = "alvenqis-desktop-v2"
version = "2.0.0"
description = "Alvenqis Control Center - Tauri desktop shell"
authors = ["Alvenqis Network <admin@alvenqis.network>"]
edition = "2021"
publish = false

# Keep this crate outside the monorepo Cargo workspace (Tauri manages its own graph).
[workspace]

[lib]
name = "alvenqis_desktop_v2_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-notification = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
dirs = "6"
thiserror = "2"
url = "2"
tokio = { version = "1", features = ["process", "time", "io-util", "rt-multi-thread", "sync", "macros"] }
parking_lot = "0.12"
regex = "1"
open = "5"
# Shared protocol client: network defaults + typed RPC for status/account/balance.
# Snapshot still uses ad-hoc GETs for indexer/pool/fleet (not in SDK v0).
alvenqis-sdk-rust = { path = "../../alvenqis-sdk-rust", default-features = false, features = ["native"] }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
